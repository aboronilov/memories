import React from 'react';
import useStyles from './styles'
const classes = useStyles()

const Post = () => {
   const classes = useStyles()
   return (
      <div>
         Post
      </div>
   );
}

export default Post;
